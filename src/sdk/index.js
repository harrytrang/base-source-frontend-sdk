/**
 * Multi-SDK barrel: namespace imports per product.
 *
 * @example
 * import { vibex } from 'base-source-frontend-sdk/sdk';
 * const client = vibex.createClient({ serverUrl: 'https://…' });
 */
export * as vibex from "./vibex/index.js";
